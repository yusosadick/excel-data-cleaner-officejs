# excel-data-cleaner-officejs
A professional Excel Add-in that cleans and standardizes messy spreadsheet data using deterministic logic, with optional AI-powered analysis (via external API, disabled by default).
